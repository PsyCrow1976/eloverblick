# ElOverblik Customer API refresh token (never commit the real value)
ELOVERBLIK_TOKEN=

# Existing PostgreSQL server (not created by this compose file)
POSTGRES_HOST=192.168.1.130
POSTGRES_PORT=5432
POSTGRES_USER=home
POSTGRES_PASSWORD=
POSTGRES_DB=home

# FastHTML listen address inside the container
WEB_HOST=0.0.0.0
WEB_PORT=5001
